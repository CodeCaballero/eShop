# Filled at publish time (images/ports). Passwords are set at deploy time from CI secrets — never commit real values.

REDIS_PASSWORD=
EVENTBUS_PASSWORD=
POSTGRES_PASSWORD=

IDENTITY_API_IMAGE=ghcr.io/${REPO}/identity-api:${TAG}
BASKET_API_IMAGE=ghcr.io/${REPO}/basket-api:${TAG}
CATALOG_API_IMAGE=ghcr.io/${REPO}/catalog-api:${TAG}
ORDERING_API_IMAGE=ghcr.io/${REPO}/ordering-api:${TAG}
ORDER_PROCESSOR_IMAGE=ghcr.io/${REPO}/order-processor:${TAG}
PAYMENT_PROCESSOR_IMAGE=ghcr.io/${REPO}/payment-processor:${TAG}
WEBHOOKS_API_IMAGE=ghcr.io/${REPO}/webhooks-api:${TAG}
WEBHOOKSCLIENT_IMAGE=ghcr.io/${REPO}/webhooksclient:${TAG}
WEBAPP_IMAGE=ghcr.io/${REPO}/webapp:${TAG}

IDENTITY_API_PORT=8081
BASKET_API_PORT=8082
CATALOG_API_PORT=8083
ORDERING_API_PORT=8084
ORDER_PROCESSOR_PORT=8085
PAYMENT_PROCESSOR_PORT=8086
WEBHOOKS_API_PORT=8087
WEBHOOKSCLIENT_PORT=8088
WEBAPP_PORT=8080
